# FunMessageBot

A simple Python script to automate sending messages.  
Use it to send multiple messages or repeat one message with a delay.  

## Usage
1. Install pyautogui: `pip install pyautogui`
2. Run the script and focus the input box where messages should go.
